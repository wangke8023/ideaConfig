#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")
/**  
 * @ProjectName ${PROJECT_NAME}
 * @Title: ${NAME}
 * @Description: TODO
 * @author ${USER}
 * @date ${DATE} ${TIME}
*/
public enum ${NAME} {
}
